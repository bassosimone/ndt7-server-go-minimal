package main

const (
	// SpecInitialMessageSize is the initial message size
	SpecInitialMessageSize = 1 << 13

	// SpecMaxMessageSize is the maximum message size
	SpecMaxMessageSize = 1 << 24

	// SpecWebSocketProto is the websocket protocol name
	SpecWebSocketProto = "net.measurementlab.ndt.v7"
)
